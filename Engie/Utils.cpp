#include "pch.h"
#include "Utils.h"

//Utils::Utils()
//{
//}
//
//Utils::~Utils()
//{
//}


/*
template<typename T>
T* Utils::GetListElement(std::list<T*> list, const int index) {

	if (index >= 0 && list.size() > index)
	{
		typename std::list<T*>::iterator iter = list.begin();
		std::advance(iter, index);
		return *iter;

	}
	return nullptr;
}


*/

